# Wave 5 close — graph integrity (2026-09-24)

**Program:** REQ-TIED_DAE_INCORPORATION  
**Profile:** minimal / advisory  

## W5a — ontology rules

- Extended `tied_validate_consistency` with optional `ontology_rules: true` (no sibling MCP tool).
- Engine: `mcp-server/src/ontology-rules.ts` — Tarjan SCC on ARCH/IMPL `related_decisions.depends_on`, duplicate detail path detection, inverse REQ `depends_on` advisories, optional disjoint session check when `adherence_ledger` supplied.
- Fixture: `fixtures/graph/arch-cycle-minimal.yaml`
- Tests: `mcp-server/src/ontology-rules.test.ts`

## W5b — charter compliance table

- Module: `mcp-server/src/charter-compliance-table.ts`
- Checklist hook: `validateChecklistGate` at `pre_implementation` when `evidence.charterCompliance` is supplied (opt-in; defaults unchanged).
- Fixture: `fixtures/charter/immutable-req-touch.yaml`
- Tests: `mcp-server/src/charter-compliance-table.test.ts`

## Verification

```text
cd mcp-server && npm run build && npm test
→ 1036/1036 pass (2026-09-24 W5 close)
```

Interface choice: **extend** `tied_validate_consistency` (preferred path per PLAN pass 2).

Program-level machine close-out (envelope, `close_out` gate) remains **deferred** for sponsor.
