CITDP-FEAT-ORCH-BATCH-1:
  change_definition:
    current_behavior: "Batch 0 validates feature manifests, lifecycle transitions, canonical references, and fixtures but has no durable feature package, creation protocol, lifecycle command adapter, standalone CLI, or orchestration MCP surface."
    desired_behavior: "Persist feature manifests under tied/features/<feature-directory>/feature.yaml with deterministic FEAT allocation and slugging, explicit request-key idempotency, atomic revision-safe mutation, lifecycle commands, and equivalent thin CLI/MCP bindings."
    non_goals:
      - "Batch 2 clarification schema and gates"
      - "Batch 2 constitution schema and analyzer"
      - "Batch 3 task graph scheduling"
      - "Batch 4 generated views"
      - "Batch 6 migration"
      - "Git branch/worktree creation"
    success_criteria:
      - "CLI and MCP share one command adapter and do not duplicate TIED YAML CRUD"
      - "Feature creation is deterministic and retry-safe"
      - "Mutations reject stale revisions and preserve complete prior files on failure"
      - "No branch or worktree is created"
      - "specify/refine/plan/tasks/verify/close_out return current state and next permitted phase"
    unchanged_behavior: "Batch 0 manifest, lifecycle, reference, and fixture validators remain the canonical validation dependencies."
  completion_criteria:
    deferred_boundaries:
      - "Batch 2 clarification and constitution logic"
      - "Distributed filesystem stress testing"
      - "Git integration"
    module_validation: "FeatureStore/allocator independently validated with filesystem temp roots and failure paths. Command adapter independently validated with store doubles through its public boundary. CLI and MCP composition validated after module tests."
    proof_boundaries:
      - "Tests prove synchronous filesystem contract and shared adapter routing; they do not prove distributed locking or future Batch 2 gates."
    verification_gate_notes: "Focused feature-orchestration suite passed 19 tests; TypeScript build passed; IDE lints reported no errors; TIED base path confirmed before writes; final YAML and consistency validation recorded separately."
  evidence:
    commands:
      - "node --test dist/feature-orchestration/*.test.js"
      - "npm run build"
      - "pseudocode_validate"
      - "ReadLints on mcp-server/src/feature-orchestration and mcp-server/src/tools/index.ts"
      - "tied_config_get_base_path"
      - "tied_validate_consistency"
      - "yaml_index_validate"
    manifest_reference: "tied/features/<feature-directory>/feature.yaml"
    proof_boundaries:
      - "Canonical REQ/ARCH/IMPL mutation delegates to existing TIED YAML loader/update/consistency functions in the MCP registration."
      - "No branch/worktree commands were introduced or invoked."
    tests_passed: 19
  impact_analysis:
    impl_inventory_notes: "Implemented IMPL-FEAT_STORE, IMPL-FEAT_IDENTIFIER_ALLOCATOR, IMPL-FEAT_IDEMPOTENT_CREATE, IMPL-FEAT_ATOMIC_MUTATION, IMPL-FEAT_ORCHESTRATION_COMMANDS, IMPL-FEAT_ORCHESTRATION_CLI, and IMPL-FEAT_ORCHESTRATION_MCP from their sidecar pseudo-code."
    modules_or_boundaries: "FeatureStore and allocator; idempotent creation and atomic mutation; lifecycle command adapter; standalone CLI; MCP handlers and delegated canonical TIED operation."
    tied_tokens_affected:
      - "ARCH-FEAT_IDEMPOTENT_CREATION"
      - "ARCH-FEAT_IDENTIFIER_ALLOCATION"
      - "ARCH-FEAT_ORCHESTRATION_BOUNDARY"
      - "ARCH-FEAT_ORCHESTRATION_COMMANDS"
      - "ARCH-FEAT_REVISION_SAFE_MUTATION"
      - "ARCH-FEAT_STORE_PERSISTENCE"
      - "IMPL-FEAT_ATOMIC_MUTATION"
      - "IMPL-FEAT_IDEMPOTENT_CREATE"
      - "IMPL-FEAT_IDENTIFIER_ALLOCATOR"
      - "IMPL-FEAT_ORCHESTRATION_CLI"
      - "IMPL-FEAT_ORCHESTRATION_COMMANDS"
      - "IMPL-FEAT_ORCHESTRATION_MCP"
      - "IMPL-FEAT_STORE"
      - "REQ-FEAT_IDEMPOTENT_CREATION"
      - "REQ-FEAT_IDENTIFIER_ALLOCATION"
      - "REQ-FEAT_ORCHESTRATION_COMMANDS"
      - "REQ-FEAT_ORCHESTRATION_SURFACE"
      - "REQ-FEAT_REVISION_SAFE_MUTATION"
      - "REQ-FEAT_STORE_PERSISTENCE"
    tied_tokens_new: []
  leap_feedback:
    divergences_from_analysis: "Implementation selected exclusive lock directories for deterministic synchronous filesystem locking and added feature-local request-key metadata at tied/features/request-keys.yaml; no requirement or architecture scope changed."
    record_status: "implemented"
    tied_stack_updates_required:
      - "Updated IMPL detail code locations and traceability test paths for all seven Batch 1 IMPLs."
  record_identity:
    change_request_id: "CITDP-FEAT-ORCH-BATCH-1"
    recorded_at: "2026-08-13"
    title: "Batch 1 feature package and lifecycle commands"
  risk_analysis:
    abuse_cases:
      - "concurrent request-key creation"
      - "path traversal in feature directory"
      - "replay with different title"
      - "stale writer overwrite"
      - "unsafe request-key path usage"
    quality_evidence_matrix:
      - applicability: "applicable"
        attribute: "baseline-functional"
        command_or_test: "npm run build && node --test dist/feature-orchestration/*.test.js"
        evidence_method: "Focused and full Node test suites"
        limitation: "No distributed stress test"
        owner: "AI Agent"
        rationale: "All Batch 1 command and store outcomes are contract behavior."
        result: "passed"
      - applicability: "applicable"
        attribute: "external-input-security"
        command_or_test: "mcp-server/src/feature-orchestration/store.test.ts; mcp-server/src/feature-orchestration/cli.test.ts"
        evidence_method: "Focused path, normalization, and argument tests"
        limitation: "No fuzzing in Batch 1"
        owner: "AI Agent"
        rationale: "Titles, request keys, feature paths, and CLI arguments are external inputs."
        result: "passed"
      - applicability: "applicable"
        attribute: "stateful-reliability"
        command_or_test: "mcp-server/src/feature-orchestration/store.test.ts"
        evidence_method: "Store unit tests"
        limitation: "Filesystem behavior tested synchronously"
        owner: "AI Agent"
        rationale: "Atomic writes, locks, retries, and stale revisions protect feature state."
        result: "passed"
    quality_profiles:
      - "baseline-functional"
      - "data-integrity-migration"
      - "external-input-security"
      - "stateful-reliability"
    risks:
      - id: "RISK-002"
        mitigation: "Commands report deferred clarification/constitution boundaries and do not invent those schemas."
        residual: "End-to-end lifecycle enforcement remains blocked until Batch 2 integration."
        summary: "Batch 2 gates are unavailable"
      - id: "RISK-003"
        mitigation: "Focused unit and composition tests cover lock, revision, and no-partial-write behavior."
        residual: "Runtime stress testing is deferred."
        summary: "TIED consistency cannot prove runtime concurrency safety"
      - id: "RISK-001"
        mitigation: "Use deterministic exclusive lock directories and return collision/stale outcomes; add stronger platform-specific lock implementation in a later hardening pass if needed."
        residual: "Lock correctness is covered for the synchronous filesystem contract, not distributed processes."
        summary: "Cross-platform lock semantics may differ"
    scenarios:
      - "atomic publication failure"
      - "canonical TIED delegation"
      - "CLI invalid arguments"
      - "deterministic allocation"
      - "idempotent retry"
      - "lifecycle blocked transition"
      - "MCP feature-local routing"
      - "path traversal rejection"
      - "request-key collision"
      - "slug normalization"
      - "stale revision"
  tdd_sequence:
    composition: "Completed before final binding registration; CLI/MCP composition tests pass."
    e2e: "Not applicable: no UI-only behavior."
    summary: "RED tests were added before new production modules; npm run build initially failed on missing modules; GREEN implementation then passed focused tests, followed by TypeScript lint diagnostics and full build."
    unit_test_green: "Completed for store, command, CLI, and MCP modules."
    unit_test_red: "Completed with honest missing-module RED evidence."
  test_strategy:
    matrix_notes: "FeatureStore and allocator: store.test.ts; idempotent create and revision CAS: store.test.ts; lifecycle command mapping: commands.test.ts; standalone CLI binding: cli.test.ts; MCP routing and canonical delegation: mcp.test.ts; Batch 0 regression coverage remains in feature-orchestration/*.test.ts."
    summary: "Strict RED tests preceded implementation, followed by independent store/command validation and composition tests for CLI/MCP bindings. No UI E2E is justified."
