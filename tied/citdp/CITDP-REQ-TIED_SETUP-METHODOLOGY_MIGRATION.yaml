CITDP-REQ-TIED_SETUP-METHODOLOGY_MIGRATION:
  change_definition:
    current_behavior_prior: "copy_files.sh seeded vocabulary only when tied/vocab/ was empty, and the client-facing migration procedure and quality documentation were not installed by the bootstrap doc list."
    desired_behavior: "Existing TIED clients can follow tied/docs/methodology-migration.md, receive the lean migration and quality documentation pack when missing, add absent vocabulary with copy_files.sh --merge-vocab without overwriting client glossaries, and inherit the promoted quality REQ/ARCH/IMPL records and pseudo-code sidecars."
    non_goals:
      - "Automatically merging customized documentation or routing.md contents"
      - "Deleting or rewriting client project YAML"
      - "Enabling strict quality gates for every client"
    references:
      - "AGENTS.md"
      - "copy_files.sh"
      - "tied/docs/client-development-index.md"
      - "tied/docs/methodology-migration.md"
      - "tied/docs/quality-assurance-commands.md"
      - "tied/docs/quality-evidence-manifest.md"
      - "tied/vocab/routing.md"
      - "tied/vocab/tied-methodology.md"
    success_criteria:
      - "--merge-vocab adds only absent vocabulary files and preserves existing client files"
      - "Lean quality and composition docs are installed when missing"
      - "Migration guide is installed through DOCS_TO_COPY when missing"
      - "Project YAML is preserved by bootstrap"
      - "Promoted quality records and pseudo-code sidecars are copied into tied/methodology/"
      - "tied_validate_consistency passes"
  completion_criteria:
    leap_closeout_notes: "IMPL-TIED_FILES migration semantics were propagated to ARCH-TIED_STRUCTURE and REQ-TIED_SETUP; vocabulary routing and cross-topic notes record methodology migration, client refresh, and vocabulary merge mode."
    verification_gate_notes: "mcp-server npm run build and npm test passed (145 tests); webhook pilot npm test passed (7 tests); bash -n copy_files.sh passed; bootstrap fixture verified project YAML, customized docs/vocabulary, additive --merge-vocab, unrelated client content, promoted quality detail/sidecars, and stale inherited methodology pruning; changed YAML passed scripts/lint_yaml.sh; all changed pseudo-code sidecars passed Layer B pseudocode_validate; staged plumb preview reported no unregistered tokens; tied_validate_consistency returned ok true."
  impact_analysis:
    impl_inventory_notes: "Changed copy_files.sh argument parsing, vocabulary merge behavior, DOCS_TO_COPY, and implementation pseudo-code sidecar installation; added methodology-migration.md; updated client-development-index.md and tied-methodology.md; promoted quality index rows, semantic-token entries, detail YAML, and pseudo-code sidecars into templates."
    modules_or_boundaries: "Bootstrap script, inherited documentation, domain vocabulary, template TIED indexes/details, and client migration procedure. Client project YAML remains outside the mutation scope."
    tied_tokens_affected:
      - "ARCH-TIED_STRUCTURE"
      - "IMPL-TIED_FILES"
      - "PROC-VOCABULARY_INDEX"
      - "REQ-TIED_SETUP"
    tied_tokens_new: []
  leap_feedback:
    divergences_from_analysis: "The implementation adds pseudo-code sidecar copying and exact inherited-methodology refresh pruning so promoted Active quality IMPLs remain available without stale inherited files; this does not alter project YAML preservation."
    record_status: "complete"
    tied_stack_updates_required:
      - "ARCH-TIED_STRUCTURE implementation approach must define inherited snapshot refresh, project YAML preservation, additive vocabulary merge, and promoted quality sidecars"
      - "IMPL-TIED_FILES implementation approach and pseudo-code must describe additive vocabulary merge and inherited pseudo-code sidecar copying"
      - "REQ-TIED_SETUP satisfaction criteria must define migration-guide adoption, additive vocabulary merge, project YAML preservation, and promoted quality sidecar availability"
  record_identity:
    change_request_id: "REQ-TIED_SETUP-METHODOLOGY_MIGRATION"
    checklist_process: "[PROC-AGENT_REQ_CHECKLIST]"
    checklist_session_note: "change-definition: add existing-client methodology migration procedure and non-destructive bootstrap capabilities. impact-discovery: copy_files.sh, DOCS_TO_COPY, vocab routing, template quality records, and client-development-index. risk-assessment: preserve project YAML, customized docs, and vocabulary. test-strategy: build, full unit/composition suite, webhook pilot suite, shell syntax, bootstrap refresh fixture, YAML lint, Layer B pseudo-code validation, staged plumb preview, and TIED consistency. verification-gate: all observed gates passed."
    checklist_step_completed: "persist-citdp-record"
    checklist_version: "agent_req_implementation_checklist v1.7.0"
    recorded_at: "2026-08-02"
    tied_context:
      architecture:
        - "ARCH-TIED_STRUCTURE"
      implementation:
        - "IMPL-TIED_FILES"
      processes:
        - "PROC-VOCABULARY_INDEX"
      requirements:
        - "REQ-TIED_SETUP"
    title: "Non-destructive client methodology migration and additive vocabulary merge"
    workspace_root: "/Users/fareed/Documents/dev/chatgpt/stdd"
  risk_analysis:
    risks:
      - id: "RISK-MIGRATION-001"
        description: "A refresh could overwrite client-owned TIED data or customized documentation."
        likelihood: "Medium"
        mitigation: "Refresh only tied/methodology; keep project indexes/details untouched; copy docs only when missing; replace inherited methodology from the template snapshot and assert stale-file pruning."
        severity: "High"
      - id: "RISK-MIGRATION-002"
        description: "Vocabulary merge could overwrite a client glossary or silently discard terminology."
        likelihood: "Medium"
        mitigation: "--merge-vocab checks destination filenames and copies only absent files; existing routing.md content is manually merged."
        severity: "Medium"
      - id: "RISK-MIGRATION-003"
        description: "Clients could mistake structural TIED validation for runtime quality proof."
        likelihood: "Medium"
        mitigation: "Migration guide labels every validator and evidence manifest proof boundary and requires staged rollout."
        severity: "Medium"
    summary: "Risks are bounded by non-destructive bootstrap semantics, explicit base-path verification, snapshots, and proof-boundary documentation."
  tdd_sequence:
    summary: "Documentation and bootstrap change: IMPL/TIED procedure first; RED/GREEN code TDD is not applicable to the documentation-only guide, while the copy_files.sh behavior is verified by shell syntax and a bootstrap fixture; no UI E2E is required."
  test_strategy:
    coverage_gaps:
      - "Bootstrap failure modes are not separately exercised; this remains outside the documentation/bootstrap fixture scope."
      - "Full promoted-token index assertions are partial; the refresh fixture asserts directory parity and a promoted sidecar, while runtime quality validators have dedicated unit/composition tests."
      - "Merged index loader and getDetailPath have no dedicated migration test; existing bootstrap and loader smoke coverage remains indirect."
    matrix_notes: "mcp-server npm run build and npm test (145 passing); pilot/webhook-inbox npm test (7 passing); bash -n copy_files.sh; disposable-client refresh fixture with project YAML, customized docs/vocabulary, additive merge, unrelated content, promoted quality detail/sidecar, and stale methodology pruning; scripts/lint_yaml.sh per changed YAML; Layer B pseudocode_validate for all changed sidecars; staged tied_plumb_diff_impact_preview with no unregistered tokens; tied_validate_consistency with details and pseudo-code."
    summary: "Unit, composition, and E2E coverage passed for the staged scope; documented gaps are deferred without changing the migration non-goals."
