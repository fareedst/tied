# Cursor AI Agent Loader for TIED Projects

This file exists solely to direct Cursor to the canonical instructions in `AGENTS.md`. Keep that document open (or pinned in your IDE) so every session honors the same requirements.
