{
  "name": "@tied/agentstream",
  "version": "1.0.0",
  "description": "Agentstream strangler package (tiedpreflight TS port; Go pipeline shim)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/*.test.js dist/red/*.test.js",
    "test:harness-red": "npm run build && node --test dist/red/harness-select-phase2.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
