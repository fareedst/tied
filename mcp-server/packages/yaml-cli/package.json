{
  "name": "@tied/yaml-cli",
  "version": "1.0.0",
  "description": "TIED YAML lint and canonicalize CLI (delegates to @tied/mcp canonicalizer)",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/*.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
