{
  "name": "@tied/cli",
  "version": "1.0.0",
  "description": "Umbrella TIED developer CLI (mcp, bootstrap, yaml, agentstream)",
  "type": "module",
  "private": true,
  "bin": {
    "tied": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/*.test.js dist/gate-check.test.js dist/tied-next.test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
