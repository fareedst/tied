{
  "name": "tdd_state_guide",
  "description": "Guide the client through the TDD progression states. The wrapper should pass current_state via arguments.current_state. Call with no current_state to get unit-test-red; call with current_state to get the next record. Terminal state is end_tdd (is_end). Legacy end accepted as alias. Other states: unit-test-red, unit-test-green, unit-refactor, three-way-alignment-unit, composition-integration, end-to-end-ui. Unknown non-empty states return error. The response includes the selected record fields plus id, guidance, and is_end.",
  "arguments": {
    "type": "object",
    "properties": {
      "current_state": {
        "type": "string",
        "description": "Current state supplied by the wrapper as arguments.current_state. Omit or pass an empty string to receive unit-test-red. Unknown non-empty states return error."
      }
    },
    "required": [],
    "additionalProperties": false,
    "$schema": "http://json-schema.org/draft-07/schema#"
  }
}
